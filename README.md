## Setup

1. Clone the repository
2. Create these files in the project root:
   - `students.csv`
   - `attendance.csv`
3. Copy the headers from:
   - `students.sample.csv` → `students.csv`
   - `attendance.sample.csv` → `attendance.csv`

The program will automatically append data to these files.
